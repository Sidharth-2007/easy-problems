//find area of rectangle

#include <iostream>
using namespace std;
int main(){
    float len,bre,area;//defining variable for length,breath,area
    
    cout<<"Enter length of rectangle:";
    cin>>len;
    cout<<"Enter breadth of rectangle:";
    cin>>bre;
    area=len*bre;//calculating area
    cout<<"Area of rectangle:"<<area;
    
    return 0;
    
}
